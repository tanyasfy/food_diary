import { SchedulerEngine } from '../../common/SchedulerEngine';
import { CronJob } from './CronJob';
export declare class CronJobEngine extends SchedulerEngine<CronJob> {
    private readonly jobs;
    constructor();
    add(job: CronJob): void;
    stop(): void;
}
